- 2026-05-11: Refined Travel Wallet desktop visual hierarchy without changing wallet/payment logic: tightened the three-band layout (account summary, payment methods, recent activity/actions), standardized card border/elevation rhythm and tabular metadata typography, and added explicit focus-safe primary vs destructive action separation on loyalty program controls. Verified no payment-method persistence/auth logic changes by limiting edits to presentation classes in TravelWallet and running typecheck + payment normalization/balance tests.
- 2026-05-11: Refined dashboard desktop control-bar composition and TripGrid presentation without touching filtering/sorting/query contracts: desktop nav now uses 12-column cluster spacing (actions/search, view toggle, reserved create/manage lane), TripGrid card gutters are aligned for 1024/1280/1440+ with max-width capping, and loading skeleton state now matches premium card-shell styling while preserving existing sortable + keyboard/touch interaction paths.
- 2026-05-11: Standardized Event desktop tab panel shell rhythm by introducing shared `EventTabPrimitives` (consistent panel padding + header structure + loading/error surfaces), applied to Event Tasks and spacing-parity wrappers for Agenda/Line-up without changing event data mapping, tab enablement, mobile event detail routing, or permissions/realtime paths.
- 2026-05-11: Replaced desktop InviteModal custom portal shell with shared Radix dialog primitive (`Dialog`/`DialogContent`) while keeping mobile Drawer flow unchanged; preserved invite-link generation/settings wiring (approval + expiry + regenerate/retry/share/copy) and added deterministic desktop autofocus to the first actionable control (Copy button) via `onOpenAutoFocus` ref targeting. Added desktop spacing rhythm/separators to reduce panel crowding without touching invite business logic paths for demo/auth modes.
- 2026-05-11: Audited unread/badge contracts for TripChat tabs and split semantics in `useUnreadCounts`: introduced explicit `broadcastUnreadCount` (unread-only split) plus `totalBroadcastCount` (full filtered broadcast total) so Broadcasts badge now reflects canonical broadcast classifier across loaded messages, while unread count remains message-only. Updated MessageTypeBar prop naming to `broadcastBadgeCount` and aligned TripChat + chat render-path tests to prevent future unread-vs-total ambiguity.
- 2026-05-07: Resolved Index hook-order blocker for marketing cold-load split by introducing a lightweight `Index` wrapper (auth/demo + auth-modal state only) that short-circuits unauthenticated browser root traffic into a dedicated `UnauthIndex` component, while moving the existing dashboard-heavy logic into `AuthIndex` so authed-only hooks (`useTrips`, realtime, pull-to-refresh, pending requests) are never conditionally skipped. This preserves behavior and removes `react-hooks/rules-of-hooks` risk from prior early-return attempts.
- 2026-05-07: Adjusted main bootstrap host-version mismatch handling to skip hard reload on public anonymous marketing/auth routes (using the same localStorage session marker heuristic as auth bootstrap), while still clearing caches + persisting current host version; authenticated/private routes preserve reload path. Added inline tradeoff note about tolerating slightly stale session snapshot to avoid double cold-load on landing.
- 2026-04-27: Unified Stream pin mutation reconciliation into canonical `upsertMessageInState` flow by applying `togglePin` success responses (and fallback local pin projection when response payload is absent) through the same dedupe/sort path used by `message.new`, `message.updated`, and send confirmations; added pin-toggle regression assertions that pinned-tab derivation updates immediately after pin and unpin without waiting for websocket timing.
- 2026-04-28: Rebased trip-invite preview work onto `main` where `useUserTripsRealtime` already lands user-trip invalidation on reconnect (`shouldBackfillTripsOnSubscribe` after first `SUBSCRIBED`) and foreground return (`shouldRefreshTripsOnForeground` on `focus`/`visibilitychange`); conflict resolution kept that source-of-truth hook and dropped duplicate every-subscribe invalidation from the branch. Trip preview parallel `trip_members` / `trip_join_requests` reads now treat Supabase errors explicitly so transient failures do not mis-label pending users as having no request.
- 2026-04-28: Hardened `TripPreview` invite access fallback so authenticated non-members no longer dead-end when invite code hydration lags. The preview now checks both `trip_members` and the latest `trip_join_requests.status` in parallel, routes pending requesters back to Home with status guidance, and performs a one-click recheck for newly approved memberships before showing a sync toast. Added regression coverage ensuring pending-request users see a deterministic “View Request Status” CTA and are navigated home instead of staying stranded on preview.
- 2026-04-27: Added parity-focused chat pinning regression coverage across Trip/Pro/Event TripChat contexts (pin/unpin action wiring, pinned-panel immediate hydration, and search/channels tab availability) and hardened mobile segmented-chat tap targets to `min-h-11` for portrait ergonomics. Also tightened `useStreamTripChat.pin` test assertions to guarantee pin toggles send only partial pin fields (no message-text overwrite payload).
- 2026-04-27: Hardened Stream pin hydration in `streamMessageViewModel` by deriving canonical pin state from explicit `pinned` when present and falling back to `pinned_at` for payloads that omit the flag; stale `pinned_at` is now cleared when `pinned: false` arrives so Pin/Unpin menu + pinned tab reflect updates immediately across trip/pro/event chat surfaces. Added adapter regressions for pinned_at-only hydration and stale timestamp unpin payloads.
- 2026-04-27: Hardened join reliability for shared trip previews by making `get-trip-preview` optionally auto-provision an active invite code when missing (`ensureInvite`), then updating `TripPreview` to always request preview data with invite healing and retry once on Join CTA if invite code is still absent. Added invite-flow regression test for retry path and ensure-invite invocation contract.
- 2026-04-27: Hardened branded trip-share proxy resilience for `/t/:tripId` by adding a fallback HTML response in `api/trip-preview.ts` when `generate-trip-preview` returns non-HTML or non-2xx payloads (including Supabase runtime degradation JSON). Fallback now meta-refreshes and links users to `https://chravel.app/trip/:id/preview` so invite/join flow remains reachable instead of displaying raw JSON error blobs. Added regression tests validating degraded-upstream fallback and normal HTML pass-through (`src/__tests__/trip-preview-api.test.ts`).
- 2026-04-27: Tightened TripChat pin flow to rely exclusively on hook-level `togglePin` (removed local Stream client gate and misleading updateMessage log label), so pin/unpin behavior is centralized in `useStreamTripChat.togglePin` across trip/pro/event surfaces and avoids any UI-level full-message mutation path.
- 2026-04-27: Extended Stream message view-model pin projection so `streamMessageViewModel` now maps canonical Stream `pinned`/`pinned_at` into `isPinned`/`pinnedAt`, ensuring pinned tab/menu state updates directly from live message payloads. Added adapter regression tests for both pinned and unpinned mapping paths.
- 2026-04-27: Unified Stream trip-chat message reconciliation with a single `upsertMessageInState` path in `useStreamTripChat` so `message.new`, `message.updated` (including pin/unpin update echoes), and async send confirmations all share deterministic dedupe + chronological ordering; added regression coverage ensuring send confirmation + message.updated pin toggle keep stable order and in-place message updates.
- 2026-04-23: Hardened Stream thread metadata projection by deriving `hasUnreadThreadReplies` from channel read markers when thread counters are absent, kept parent-thread preview mapping deterministic, and updated MessageBubble thread CTA to always-visible 44px+ tap target (`min-h-11`) for mobile accessibility. Added adapter regression test for read-marker-driven unread detection.
- 2026-04-23: Fixed trip-preview infinite-refresh loop caused by server rewrite recursion (`/trip/:tripId/preview` -> `/api/trip-preview` -> HTML meta-refresh back to `/trip/:tripId/preview`). Removed the `/trip/:tripId/preview` Vercel rewrite so that path now resolves to SPA `TripPreview` route while branded `/t/:tripId` remains the OG proxy entry point.
- 2026-04-23: Fixed dashboard Requests-tab drift by making pending `useTrips` rows (`membership_status='pending'`) the canonical source for request cards/counts, wiring `Index` to pass scoped/searchable pending trip cards + outbound request-id map into `TripGrid`, and falling back to `dashboardJoinRequests` only when pending trips are unavailable. Added regression test ensuring pending trip cards render in Requests mode with pending-action controls.
- 2026-04-23: Fixed branded trip-share join regression where `/t/:tripId` unfurl pages on `p.chravel.app` could loop back to static OG preview instead of interactive join flow. `api/trip-preview` now forwards canonical URL and forces `appBaseUrl=https://chravel.app` when proxying to `generate-trip-preview`, app router now supports `/t/:tripId` as a TripPreview alias, and regression test coverage now verifies `/t/:tripId` still routes non-members to `/join/:code`.
- 2026-04-23: Extended Stream message view-model threading metadata projection by mapping parent `reply_count`, `latest_replies` preview text, and optional thread unread/participant markers into `replyCount`, `threadPreviewSnippet`, and `hasUnreadThreadReplies` while preserving top-level timeline filtering (`!parent_id`/`!reply_to_id`). Added adapter regression test coverage to assert parent reply counts/snippets and exclusion of child replies from timeline.
- 2026-04-23: Added `scripts/check-stream-config-parity.cjs` and npm alias `ops:check-stream-parity` to verify frontend Stream/Supabase env parity plus webhook secret/x-api-key alignment using masked digests, enabling deterministic preflight checks for mention/recovery failures caused by config drift.
- 2026-04-23: Restored chat production-readiness gate stability after `useStreamTripChat` started requiring `useAuth` context by adding explicit `@/hooks/useAuth` test mocks in `useStreamTripChat.send` and `useStreamTripChat.reactions` suites; `npm run qa:chat-production-readiness` now passes again (36/36 tests).
- 2026-04-23: Fixed dashboard Requests-tab drift by making pending `useTrips` rows (`membership_status='pending'`) the canonical source for request cards/counts, wiring `Index` to pass scoped/searchable pending trip cards + outbound request-id map into `TripGrid`, and falling back to `dashboardJoinRequests` only when pending trips are unavailable. Added regression test ensuring pending trip cards render in Requests mode with pending-action controls.
- 2026-04-23: Fixed branded trip-share join regression where `/t/:tripId` unfurl pages on `p.chravel.app` could loop back to static OG preview instead of interactive join flow. `api/trip-preview` now forwards canonical URL and forces `appBaseUrl=https://chravel.app` when proxying to `generate-trip-preview`, app router now supports `/t/:tripId` as a TripPreview alias, and regression test coverage now verifies `/t/:tripId` still routes non-members to `/join/:code`.
- 2026-04-23: Completed post-stabilization concierge transport cleanup by removing unused Stream concierge scaffolding (`getOrCreateConciergeChannel`, unsupported concierge guard exports, and concierge-only guard tests), keeping Stream transport helpers focused on active config/state checks only, and updating boundary documentation to reflect the reduced typed Stream abstraction surface.
- 2026-04-23: Enforced Stream-canonical chat mutation path in UI by forwarding `transportMode` into `MessageActions` from `MessageBubble` (preventing accidental legacy DB edit/delete calls in Stream surfaces), extracting legacy chatService edit/delete fallback behind `features/chat/services/legacyMessageMutations.ts`, and adding ESLint restricted-import guardrails that block `@/services/chatService` imports in chat/pro-channel/stream UI modules (tests excluded).
- 2026-04-23: Added enforceable trip/pro chat production-readiness gate (`qa:chat-production-readiness`) backed by `qa/journeys/chat-production-readiness.json` and `scripts/qa/run-chat-production-readiness-gate.cjs` to freeze new chat feature work until token/connect, membership/role, reconnect mutation resilience, and fallback-bound checks all pass in one deterministic command.
- 2026-04-21: Codified concierge transport boundary as SSE/DB-only by deprecating Stream concierge channel factory usage (`getOrCreateConciergeChannel` now throws deterministic unsupported error), removing concierge stream barrel exports/mappers, adding transport-guard regression tests, and documenting architecture in `docs/CONCIERGE_TRANSPORT_BOUNDARY.md`.
- 2026-04-21: Centralized Stream read-state projection by adding `readStateSelectors` (message read-status mapping + unread split helper), switched `TripChat` to selector-driven read status rendering, narrowed `useChatReadReceipts` to debounce/schedule read side-effects only, and expanded Stream unread-count tests for stale windows, invalid timestamps, and sender-self filtering.
- 2026-04-22: Addressed PR #356 review finding where reconnect backfill retry could no-op when pagination was exhausted. `ThreadView` now tracks pagination vs backfill error origin and routes Retry to silent initial backfill fetch for reconnect failures. Added regression test covering reconnect-failure retry path with exhausted pagination state.
- 2026-04-21: Refactored `ThreadView` thread state management to sort replies by `created_at`, dedupe by message id, support Stream `getReplies` pagination via `id_lt`, add initial/pagination retry + loading/error/empty/backfill UI states, and trigger silent thread backfill on Stream reconnect. Added `ThreadView` regression tests for message.new/update/delete, dedupe/order guarantees, pagination error/retry boundaries, and reconnect backfill fetch behavior.
- 2026-04-21: Hardened message edit/delete transport boundaries by adding explicit `transportMode` to `MessageActions` with Stream-only callback enforcement (`onEdit/onDelete` required in stream mode), wiring TripChat + Pro ChannelChatView to pass stream mode callbacks, and keeping legacy DB mutation paths reachable only under explicit legacy mode. Added regression tests to assert Stream mode edit/delete never call `chatService.editChatMessage/deleteChatMessage`.
- 2026-04-21: Added Stream Pro channel backward pagination (`hasMore`, `isLoadingMore`, `loadMore` with `id_lt`) in `useStreamProChannel`, refactored `ChannelChatView` to consume shared stream message/reaction adapter utilities, replaced local Stream optimistic reactions with pending-intent overlay reconciled against server echoes, and added regression tests for Pro pagination plus reaction echo consistency.
- 2026-04-21: Exposed role-gated chat moderation actions in message menus for authorized trip roles (hide message, shadow ban user, mute user, ban user), wired execution through new `stream-moderation-action` edge function with server-side authorization checks, Stream mutations, and `admin_audit_logs` inserts, and added structured telemetry (`moderation_action_executed` / `moderation_action_failed`) plus UI regression tests covering moderation action visibility + invocation.
- 2026-04-17: Fixed Gmail connector initiation failure on `https://chravelapp.com` by adding `chravelapp.com` production origins to edge-function CORS allowlist (`_shared/cors.ts`) and extending CORS security tests to cover those domains.
- 2026-04-16: Fixed Stream chat ReadChannel error surfacing in Messages tab by adding membership self-heal path. Added `stream-ensure-membership` edge function (auth + trip member verification + Stream addMembers) and retry-once flow in `useStreamTripChat` with sanitized fallback messaging.
- 2026-04-16: Fixed TripChat channel-state collision causing MessageTypeBar runtime crash (`channelName.toLowerCase` on Stream channel). Split stream vs role channel state references and hardened MessageTypeBar channel label formatting against undefined names.
- 2026-04-16: Fixed installed-app auth regression that redirected iOS/Android/PWA users to external OAuth browser flows. `useAuth` now hard-blocks Google/Apple OAuth in installed contexts and `AuthModal` hides external OAuth buttons while keeping inline email/password auth available on Home.
- 2026-04-17: Updated web auth modal OAuth layout so Apple and Google sign-in options render side-by-side in browser contexts while installed-app contexts continue to suppress external OAuth buttons.
- 2026-04-16: Hardened Stream chat reliability and UX in TripChat flow: prevented empty re-watch from clobbering hydrated messages, awaited stream-join-channel with timeout, fixed reaction stale-closure handling via refs, enabled thread modal opening via explicit Open thread action with main-timeline parent filtering, removed chat-mode typing gate, and auto-cleared failed-send ghosts on matching Stream message.new echoes.
- 2026-04-16: Fixed installed-app auth regression that redirected iOS/Android/PWA users to external OAuth browser flows. `useAuth` now hard-blocks Google/Apple OAuth in installed contexts and `AuthModal` hides external OAuth buttons while keeping inline email/password auth available on Home.
- 2026-04-16: Fixed thread reply badge action in MessageBubble to open ThreadView via onOpenThread instead of inline-reply handler, aligning visible replyCount CTA with Stream parent_id thread persistence path.
- 2026-04-17: Fixed Stream join timeout surfacing as user-facing chat error (`signal is aborted without reason`) by treating stream-join-channel fetch failures/aborts as best-effort and continuing channel.watch; added regression test covering aborted join fetch path.
- 2026-04-17: Added Stream send fallback for CreateMention permission errors: when mention payload is denied (error code 17), retry message send without `mentioned_users` to prevent false failed-send toasts while preserving message delivery.
- 2026-04-17: Event chat policy simplified to always-open (`everyone`) for all attendees, removed chat-permission controls from Event Admin UI, and added Stream client self-heal connect attempt inside `useStreamTripChat` to avoid event chat timeout when app-level Stream init lags.
- 2026-04-17: Hardened Gmail OAuth connector reliability by preserving same-origin callback context. Frontend now sends origin-derived redirect URI to gmail-auth/connect; edge function validates against allowlist (GOOGLE_REDIRECT_URI + GOOGLE_ADDITIONAL_REDIRECT_URIS), signs redirect URI into OAuth state, and reuses it during token exchange to prevent cross-origin auth/session mismatch failures in preview/local environments.
- 2026-04-18: Updated stream-webhook message.new fanout for trip/broadcast/channel to mention-only recipients from Stream `mentioned_users`, switched notification-center rows to `mention`, and added notification insert dedupe on recipient+type+metadata.stream_message_id to handle webhook retries while preserving concierge/member fanout behavior.
- 2026-04-18: Hardened Stream trip chat membership recovery determinism: structured join failure reasons, validated stream-ensure-membership response before retrying ReadChannel watch, mapped membership failures to actionable UI errors with one guarded reload attempt, standardized stream-join/ensure edge-function error payloads (code/reason), and added regression tests for successful recovery + stable failure error state.
- 2026-04-18: Hardened NotificationsDialog click routing with metadata-first trip resolution (`trip_id`/`trip_type`/`channel_type`), forced mention deep-links to `?tab=chat` across consumer/pro/event routes, and added `chatNavigationContext` route state handshake to support post-navigation chat initialization readiness. Added regression tests for consumer/pro/event mention routing plus missing-metadata fallback safety.
- 2026-04-18: Hardened Stream trip-chat reaction toggling determinism in `useStreamTripChat` by tracking local own-reaction intent across rapid toggles; added regression coverage for add/remove without waiting for reaction events and picker emoji id mapping fallback tests in `MessageReactionBar`.
- 2026-04-19: Added actionable reaction-policy error surfacing in useStreamTripChat toggle path so Stream dashboard misconfiguration/allowlist blocks now show explicit admin guidance to users; covered by new reactions test.
- 2026-04-19: Replaced the "hard-block Google/Apple OAuth in installed contexts" behavior (PRs #309/#311/#320) with an in-app OAuth path that (a) removes the early-return guards in `useAuth.signInWithGoogle`/`signInWithApple`, (b) un-hides the Google/Apple buttons in `AuthModal` for installed contexts, (c) targets `https://chravel.app/auth-callback` Universal Link when installed so the native wrapper can deep-link back into the WebView, and (d) launches the OAuth URL via new `openInstalledAuthBrowser` helper that prefers `window.Capacitor.Plugins.Browser.open` (SFSafariViewController / Chrome Custom Tabs) with `window.location.assign` fallback. Added `/auth-callback` route alias to `AuthPage`. Required prerequisite: native shell (`chravel-mobile`) must register `@capacitor/browser`, the `chravel://` URL scheme, and an `appUrlOpen` listener that routes `chravel.app/auth-callback` deep links back into the WebView — otherwise Google will reject with `disallowed_useragent` in embedded WebView redirects.
- 2026-04-19: Stream webhook hardening follow-up: retired chravel-concierge channel handling, added UUID guards for recipient/trip IDs before notifications upsert, replaced check-then-insert with upsert(ignoreDuplicates) keyed by stream_message_id_mv, added migration for generated-column unique index, updated eventRouting tests, and added local B2 webhook v2 script under scripts/b2-webhook-test/.
- 2026-04-21: Added Stream chat pin/unpin support in message actions for admin/moderator roles, introduced pinned messages filter + inline pinned panel in TripChat, and added regression tests for pin permission visibility plus pinned list consistency helper behavior.
- 2026-04-22: Clarified Broadcasts vs Pinned semantics in MessageTypeBar with active-tab helper hints/tooltips to reduce overlap confusion while keeping both surfaces (option 1), plus regression test coverage for both hint states.
- 2026-04-21: Extended Stream trip/pro chat payload and render mapping to include `quoted_reference` metadata alongside `parent_id`, enabling compact quote blocks (author/time/text) for top-level quoted replies even when parent linkage is unavailable (legacy/non-thread contexts). Added parser round-trip tests for quoted metadata extraction from Stream top-level + `extra_data`.
- 2026-04-21: Enhanced trip/pro chat threading UX and telemetry: parent message thread badges now surface Stream reply_count with inline latest-reply snippet + unread thread indicator, chat search results deep-link thread replies to parent thread open, notification-driven chat deep links can auto-open thread context, and telemetry now tracks thread_opened + thread_reply_sent adoption events.
- 2026-04-21: Audited Stream mention capability path end-to-end: enabled `create-mention` grants for chravel-trip/broadcast/channel in `stream-setup-permissions`, replaced mention send fallback from error-regex-only behavior to explicit channel capability/config checks with guarded fallback retention, and added mention integration tests for send payload behavior plus webhook preference filtering and notification row creation.
- 2026-04-21: Added explicit SupabaseStream role contract validator for pro channels (`roleGrantMembershipContract`), wired validation/reporting into `useRoleAssignments` assign/remove flows, and added contract tests for membership + posting-rights drift on role changes.
- 2026-04-21: Hardened Stream typing indicators by gating legacy TypingIndicatorService fallback to explicit legacy transport mode only, adding throttled outbound Stream keystroke signaling with debounced stopTyping, clearing local typing users on Stream disconnect/channel switch, and adding regression tests for typing start/stop reconnect + transport transitions in useChatTypingIndicators.
- 2026-04-21: Added server-side Stream membership reconciler (stream-reconcile-membership) for chravel-trip + chravel-broadcast validation/repair, introduced canonical reasonCode fields in stream-join-channel and stream-ensure-membership responses, and hardened useStreamTripChat to one deterministic ensure-membership retry with structured telemetry + regression tests for join-timeout recovery, membership denied, and successful deterministic recovery.
- 2026-04-22: PR #354 audit follow-up: fixed stream-reconcile-membership global GET determinism/coverage risk by replacing non-deterministic trip_members limit scan with cursor-based ordered trips batching (`batchSize`/`cursor`/`nextCursor`), capped batch size to bound runtime, and sanitized 500 error responses while preserving server logs for diagnostics.
- 2026-04-21: Extracted Stream message-to-TripChat UI projection into pure adapter (`src/features/chat/adapters/streamMessageViewModel.ts`) and switched `TripChat` to memoized adapter usage so reaction/read-state/attachment/link-preview mapping no longer lives inline in component render logic. Added focused adapter unit coverage for edited messages, attachments/link previews, reaction aggregation, parent reply resolution, and Stream read-state projection.
- 2026-04-21: Hardened trip-cover production parity by adding idempotent migration `20260421171000_enforce_trip_cover_member_permissions.sql` to normalize `trip-covers` bucket/public config and active-member RLS for storage/trips cover writes; unified share links to `buildTripPreviewLink` defaulting to `https://chravel.app` (avoids branded-domain drift), and hardened `api/trip-preview` proxy to force `text/html; charset=utf-8` when upstream body is HTML to prevent raw-source/mojibake desktop previews. Added regression tests for orphan cleanup on DB-write failure, no-row permission failure, and trip-preview content-type coercion.
- 2026-04-21: Added follow-up migration `20260421191000_trip_cover_event_admin_only.sql` to align cover-photo authorization with product intent: consumer/pro trips allow active members, event trips restrict to creator or trip_admins, applied consistently to both storage object policies and `trips.cover_image_url` UPDATE RLS.
- 2026-04-21: Centralized Stream message search into `streamMessageSearch` wrapper used by both trip-level and multi-trip universal search; unified per-channel pagination/error handling + aggregation caps, standardized message/thread deep-link metadata payloads for universal search hits, and added regression tests for empty query guards, disconnected client behavior, and multi-channel aggregation limits.
- 2026-04-23: Added chat reliability telemetry instrumentation for dot-notation events (`message.send.failed`, `membership.recovery.attempt`, `chat.reconnect.backfill`, `chat.time_to_first_message`) in `useStreamTripChat` + telemetry helpers; added Stream webhook dedupe-collision persistence/log marker (`stream:webhook_dedupe_collision` / `webhook.dedupe.collision`), and documented 7-day daily triage dashboard runbook in `docs/ops/chat-reliability-triage-first-7-days.md`.
- 2026-04-23: Added Stream canary guardrails for trusted/internal cohort rollout (`stream_changes_canary` flag + user-hash bucketing), instrumented trip-chat incident reporting for ReadChannel denials/send failures/reconnect backfill failures/mention fallback, and introduced `stream-canary-guard` edge function that auto-disables canary by flipping feature flag off when rolling threshold rates are exceeded.
- 2026-04-23: Updated Stream chat reliability runbook (`docs/ops/chat-reliability-triage-first-7-days.md`) with explicit first 24–72h hourly monitoring for send failures, ReadChannel recovery failures, and webhook 401/500s; added rollback-toggle instructions (`stream_changes_canary`) and rollout expansion gates across trip/pro/reactions/mentions/threads.
- 2026-04-23: Updated message menu thread actions to `Reply in thread` / `View thread`, and added post-send thread-reply success affordance (`Reply sent • View thread`) in TripChat using shared `threadReplySuccess` helpers so only thread replies surface CTA while top-level sends remain unchanged. Added regression tests for menu labels and thread reply CTA state/CTA click behavior helpers.
- 2026-04-23: Hardened Stream message delete/edit UI permissions by normalizing channel own_capabilities aliases (kebab/camel), threading canDeleteOwn/canDeleteAny/canUpdateOwn through TripChat → MessageItem → MessageActions, adding 403-specific delete denial toast copy, and expanding TripChat.deleteMessage tests for owner/capability and admin-without-grant visibility.
- 2026-04-23: Clarified thread action semantics in MessageActions ("Reply in thread" + "Open thread"), added Stream-only reply-send thread affordance in TripChat (auto-opens parent thread after successful reply send), fixed latent TripChat stream reaction reference regression (reactions from useChatReactions), and added regression coverage for thread action labels/callbacks plus reply-vs-top-level send behavior.
- 2026-04-23: Consolidated TripChat message timeline rendering to a single canonical VirtualizedMessageContainer render callback with unified reactions/read-status fallback precedence, fixed pinned-banner/list JSX boundary safety, and added focused TripChat render-path regression test validating single message-list instance + thread/reply callback wiring.
- 2026-04-26: Resolved semantic merge debt hotspots by preventing Stream edit/delete double-callback invocation in MessageActions, centralizing pro/demo channel trip detection via src/constants/demoTrips.ts, aligning concierge live-voice gate to env-driven VOICE_LIVE_ENABLED, fixing LiveKit agent pending action inserts to use trip_pending_actions.tool_name + optional tool_call_id idempotency key, and unifying edge entitlement selection (create-trip/restore-trip) with shared primary-row policy. Added regression tests for stream callback single-invocation, demo trip helper, and entitlement primary selection.
- 2026-04-26: Phase-2 follow-up reduced duplicated join-request mutation paths by making TripGrid require dashboard cancel handler (no inline trip_join_requests delete fallback), and removed duplicate Stream channel ownership from useRoleChannels so ChannelChatView remains the sole pro-channel Stream transport owner.
- 2026-04-26: Added explicit entitlement semantics flags (`isPaid`, `isExplorer`, `isFrequentChraveler`, `isOrgPro`) via shared planFlags helper, aligned useSubscription/useUnifiedEntitlements/store semantics around org-pro vs paid access, and switched ArchivedTripCard gating from ambiguous `isPro` to `isPaid` to avoid Explorer lockout drift.
- 2026-04-27: Fixed Stream chat pin/unpin mutation path to use partial message updates (`partialUpdateMessage` via `useStreamTripChat.togglePin`) so pin state changes no longer risk payload replacement and original message text stays intact. TripChat now calls hook-level togglePin and regression tests cover hook partial-update payload + TripChat wiring that avoids direct `streamClient.updateMessage` for pin actions.
- 2026-04-27: Added low-noise Stream chat send-path regressions for immediate pin/unpin visibility by asserting message.updated snapshots flow into pinned derivation without delay, plus locked broadcast payload behavior (`message_type: "broadcast"`) to guard against accidental send contract drift.
- 2026-04-27: Added cross-surface pin parity regression coverage by extending TripChat render-path tests for consumer/pro/event contexts to assert moderator pin capability exposure, immediate pinned hydration (`isPinned`/`pinnedAt`) from Stream view-model payloads, and unchanged default broadcasts filter state. Expanded MessageActions stream-callback tests to cover Pin/Unpin visibility + callback behavior under capability-gated stream mode.
- 2026-04-27: Removed pinned-tab filter drift by routing `useChatComposer.filterMessages` pinned branch through canonical `derivePinnedMessages` (same pin predicate + Stream mixed snapshot dedupe path), tightened pinned utility typing to shared `PinnedChatMessage`, and extended pinned predicate test coverage for missing `isPinned` flags.
- 2026-04-28: Fixed pending join-request dashboard hydration drift by replacing `TripGrid`’s placeholder outbound fallback mapping (`Trip` / `Destination TBD` / `Date TBD` / `0` people) with a typed request-to-card mapper that preserves real trip metadata (name, destination, start/end date range, cover image) and a non-zero-safe people floor. Also aligned request stat counters in `Index` with outbound request rows when pending trips are not projected yet, preventing the `0 Requests` + visible cards mismatch. Invite/preview date rows now use the shared `CalendarGlyph` so the date icon no longer degrades to a square glyph on invite surfaces. Added mapper regression tests for hydrated and sparse metadata cases.
- 2026-04-29: Replaced pending-request dashboard fallback architecture with canonical requester-scoped Supabase RPC flow (`get_my_pending_trip_request_cards`) returning safe trip metadata + member/place counts per pending request. `Index` now derives both request cards and request counters from the same `usePendingRequestTripCards` query source (no `Math.max` count hack, no outbound placeholder projection), `TripGrid` Requests tab now renders only hydrated pending cards (no join-request fallback mapper), and join/cancel flows invalidate the shared `pending-request-trip-cards` cache key. Added hook-level regressions for hydration and missing-title fail-closed behavior plus updated Requests-grid tests to enforce no fallback rendering from raw dashboard join-request rows.
- 2026-05-09: Removed execute-concierge-tool service-role bearer bypass and replaced LiveKit path with short-lived signed agent assertions (`X-Agent-Assertion`) verified server-side (user_id/trip_id/allowed_tools/exp). livekit-token now mints assertion per room and agent forwards it when calling execute-concierge-tool, with tripId claim match + allowed-tools enforcement before execution.
- 2026-05-10: Follow-up hardening on agent assertion rollout: removed stale `executeFunctionCall`/`userId` remnants from execute-concierge-tool and replaced wildcard assertion scope with explicit external-tool allowlist minted by livekit-token to keep signed claims least-privilege.
- 2026-05-09: Added durable execute-concierge-tool idempotency for mutating tools by introducing concierge_tool_idempotency reserve/replay store keyed on (idempotency_key,user_id,trip_id,tool_name), wiring pre-execution reservation + duplicate replay short-circuit, and persisting completed result payload/reference for deterministic write dedupe across voice/text retries.

- 2026-05-12: Completed AI Concierge voice architecture audit comparing LiveKit+Gemini vs OpenAI Realtime; documented current transport/tooling split, decision matrix, phased migration plan (D→C→E), rollback strategy, and external service checklist in docs/ai-concierge-voice-migration-audit-2026-05-12.md.

- 2026-05-12: Implemented provider-flagged OpenAI Realtime voice path (default openai) with new Supabase edge function create-openai-realtime-session (auth + trip membership + ephemeral session), frontend WebRTC hook useOpenAIRealtimeVoice, and useConciergeVoice runtime selector while preserving LiveKit fallback.

- 2026-05-12: Updated OpenAI Realtime voice defaults to Onyx (`OPENAI_REALTIME_VOICE` fallback and frontend session request default) per product direction; preserved explicit per-session override support.
